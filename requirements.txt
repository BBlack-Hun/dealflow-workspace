# Web app + mock agent (Docker image). No Windows-only deps here so the image
# builds cleanly on Linux/macOS.
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
requests==2.32.3
pyyaml==6.0.2
# tests
pytest==8.3.4
httpx==0.28.1
python-multipart==0.0.20
# 투자사 현황 업로드 / 표 엑셀 내려받기 (순수 파이썬 — 컴파일러 불필요)
openpyxl==3.1.5
